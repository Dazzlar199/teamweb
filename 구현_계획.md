# 특별시 팀 대시보드 구현 계획

> **목적**: 4명 팀을 위한 내부 협업 도구  
> **인증**: 관리자 링크 기반 접근 제어

---

## 🎯 핵심 기능

### 1. 인증 시스템
- **링크 기반 인증**: 관리자가 생성한 링크로만 접근
- **Supabase Auth** 사용
- **4명만 허가**: 관리자가 링크 공유

### 2. 일정 관리
- 캘린더 뷰
- 일정 등록/수정/삭제
- 팀원별 일정 표시
- 알림 기능 (선택)

### 3. 작업 관리
- 작업 등록/수정/삭제
- 상태 관리 (할 일, 진행 중, 완료)
- 담당자 지정
- 우선순위 설정

### 4. 파일 공유
- 파일 업로드 (다양한 형식)
- 파일 다운로드
- 파일 목록 조회
- 파일 검색

### 5. 이미지 공유
- 이미지 업로드
- 이미지 다운로드
- 이미지 갤러리 뷰
- 이미지 미리보기

### 6. 대시보드
- 전체 일정 요약
- 진행 중인 작업
- 최근 업로드 파일
- 팀원 활동

---

## 🛠️ 기술 스택

- **Frontend**: Next.js 16 (App Router)
- **Styling**: Tailwind CSS
- **Database**: Supabase (PostgreSQL)
- **Storage**: Supabase Storage
- **Auth**: Supabase Auth (링크 기반)
- **Deployment**: Vercel (무료)

---

## 📁 프로젝트 구조

```
apps/team-dashboard/
├── app/
│   ├── (auth)/
│   │   └── login/          # 링크 기반 로그인
│   ├── dashboard/          # 대시보드
│   ├── calendar/           # 일정 관리
│   ├── tasks/              # 작업 관리
│   ├── files/              # 파일 공유
│   ├── images/             # 이미지 공유
│   └── layout.tsx
├── components/
│   ├── calendar/
│   ├── tasks/
│   ├── files/
│   └── ui/
├── lib/
│   ├── supabase/
│   └── utils/
└── types/
```

---

## 🗄️ 데이터베이스 스키마 (Supabase)

### users (팀원)
```sql
- id (uuid, primary key)
- email (text)
- name (text)
- role (text) -- 'admin' | 'member'
- created_at (timestamp)
```

### events (일정)
```sql
- id (uuid, primary key)
- title (text)
- description (text)
- start_date (timestamp)
- end_date (timestamp)
- user_id (uuid, foreign key)
- created_at (timestamp)
```

### tasks (작업)
```sql
- id (uuid, primary key)
- title (text)
- description (text)
- status (text) -- 'todo' | 'in_progress' | 'done'
- priority (text) -- 'low' | 'medium' | 'high'
- assigned_to (uuid, foreign key)
- created_by (uuid, foreign key)
- created_at (timestamp)
- updated_at (timestamp)
```

### files (파일)
```sql
- id (uuid, primary key)
- name (text)
- file_path (text) -- Supabase Storage path
- file_size (bigint)
- file_type (text)
- uploaded_by (uuid, foreign key)
- created_at (timestamp)
```

---

## 🚀 구현 단계

### Phase 1: 기본 구조 (1-2일)
- [x] 프로젝트 생성
- [x] 브랜드 스타일 적용
- [ ] Supabase 설정
- [ ] 기본 레이아웃

### Phase 2: 인증 (1일)
- [ ] 링크 기반 인증 구현
- [ ] 관리자 페이지 (링크 생성)

### Phase 3: 일정 관리 (2-3일)
- [ ] 캘린더 UI
- [ ] 일정 CRUD
- [ ] 일정 표시

### Phase 4: 작업 관리 (2-3일)
- [ ] 작업 목록 UI
- [ ] 작업 CRUD
- [ ] 상태 관리

### Phase 5: 파일 공유 (2-3일)
- [ ] 파일 업로드
- [ ] 파일 다운로드
- [ ] 파일 목록

### Phase 6: 이미지 공유 (1-2일)
- [ ] 이미지 업로드
- [ ] 이미지 갤러리
- [ ] 이미지 다운로드

### Phase 7: 대시보드 (1-2일)
- [ ] 대시보드 UI
- [ ] 통계/요약

---

## 🎨 디자인 가이드

### 컬러
- **Primary**: `#E8A4B8` (로즈핑크)
- **Background**: `#1A1A1D` (다크)
- **Card**: `#252528`
- **Text**: `#FAFAFA` (화이트)
- **Text Gray**: `#9CA3AF`

### 폰트
- **Primary**: Pretendard (한글 최적화)
- **Fallback**: -apple-system, sans-serif

---

**작성일**: 2025-01-05

